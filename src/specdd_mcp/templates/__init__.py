"""Packaged bootstrap templates.

These files are the canonical SpecDD bootstrap assets that ``specdd-mcp
bootstrap`` drops into a target repo. They are bundled as package data and
read via :func:`importlib.resources.files` so they resolve whether the
package runs from source or an installed wheel.
"""
